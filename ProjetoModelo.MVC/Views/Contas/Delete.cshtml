@model Moneta.Application.ViewModels.ContaViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ContaViewModel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ContaId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ContaId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descricao)
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Remover" class="btn btn-primary" />
            <span class="btn btn-default" onclick="javascript: window.history.back();">Cancelar</span>
        </div>
    </div>
    }
</div>
